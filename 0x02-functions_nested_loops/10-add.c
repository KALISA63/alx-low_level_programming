#include "main.h"
/**
 * add - adds two integers and returns their sum
 * @x: variable replaced by an integer
 * @y: variable replaced by an integer
 *
 * Return: an integer
 */
int add(int x, int y)
{
	return (x + y);
}
